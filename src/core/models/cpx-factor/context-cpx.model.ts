/**
 * The Complexity Category "Context"
 */
export class ContextCpx {

    ctxt ?= 0;                      // Modification or difficulty relative to the context (.apply(), .call(), .call())
    closure ?= 0;                   // Closures

}
