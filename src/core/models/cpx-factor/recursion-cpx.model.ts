/**
 * The Complexity Category "Recursion"
 */
export class RecursionCpx {

    callback ?= 0;                         // Callback (call to a parameter of the parentFunction (ie method) of the node)
    recursivity ?= 0;                      // Recursive method (call to the node's method)

}
