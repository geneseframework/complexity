import { Infos } from './infos.model';
import { ArgumentList } from './argument-list.model';

export class UnqualifiedExplicitConstructorInvocationChildren {
    Super?: Infos[] = [new Infos()];
    argumentList?: ArgumentList[] = [new ArgumentList()];
}
