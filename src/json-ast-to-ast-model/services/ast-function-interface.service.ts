import { AstFile } from '../../core/models/ast/ast-file.model';
import { FunctionInterface } from '../../core/interfaces/ast/function.interface';

export class AstFunctionInterfaceService {

    static generate(astFile: AstFile): FunctionInterface[] {
        return [];
    }
}
