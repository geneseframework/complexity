<script>
    var ctxBarCognitive = document.getElementById('bar-cognitive-cpx').getContext("2d");
    var barCognitive = new Chart(ctxBarCognitive, {
        type: 'bar',
        data: {
            labels: [
                {{#each stats.barChartCognitive.data}}
                    '{{this.x}}',
                {{/each}}
            ],
            datasets: [{
                data: [
                    {{#each stats.barChartCognitive.data}}
                        {{this.y}},
                    {{/each}}
                ],
                backgroundColor: [
                    {{#each stats.barChartCognitive.data}}
                        '{{this.color}}',
                    {{/each}}
                ],
            }]
        },
        options: {
            legend: {
                display: false
            },
            scales: {
                xAxes: [{
                    gridLines: {
                        display: false
                    },
                    scaleLabel: {
                        display: true,
                        labelString: 'Cognitive complexity'
                    },
                    ticks: {
                        suggestedMin: 0
                    }
                }],
                yAxes: [{
                    scaleLabel: {
                        display: true,
                        labelString: 'Number of methods'
                    },
                    ticks: {
                        suggestedMin: 0
                    }
                }]
            }
        }
    });
</script>
