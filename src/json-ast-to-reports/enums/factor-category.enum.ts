export enum FactorCategory {

    AGGREGATION = 'aggregation',
    ATOMIC = 'atomic',
    CONTEXT = 'context',
    DEPTH = 'depth',
    NESTING = 'nesting',
    RECURSION = 'recursion',
    STRUCTURAL = 'structural',
    USE = 'use'

}
